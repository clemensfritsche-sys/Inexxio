export default function ErpPage() {
  return (
    <div style={{ padding: '48px 0' }}>
      <div className="ix-wrap">
        <h1 style={{ font: '700 28px/1.2 var(--font-display)', color: 'var(--fg-1)', marginBottom: 8, letterSpacing: '-0.03em' }}>
          ERP
        </h1>
        <p style={{ font: '15px var(--font-body)', color: 'var(--fg-3)', marginTop: 4 }}>
          Inhalt folgt.
        </p>
      </div>
    </div>
  );
}
