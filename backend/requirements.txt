fastapi==0.115.5
uvicorn[standard]==0.32.1
sqlalchemy==2.0.36
psycopg2-binary==2.9.10
alembic==1.14.0
pydantic==2.10.2
pydantic-settings==2.6.1
email-validator==2.2.0
firebase-admin==6.6.0
# Passkeys / WebAuthn (FIDO2): serverseitige Register-/Login-Zeremonien.
webauthn==2.5.2
# httpx: Starlettes TestClient fährt darauf – ohne ihn läuft kein einziger Wächter.
httpx==0.28.0
# python-multipart: FastAPI braucht es für UploadFile (Bild-Anhänge der Datenerfassung).
python-multipart==0.0.17
# pillow: verkleinert und normalisiert die aufgenommenen Bilder (services/attachments).
pillow==11.0.0
# stripe: der eine Zahlungsdienst (services/stripe_pay). **Optional zur Laufzeit** – ohne
# STRIPE_SECRET_KEY wird das Modul nie importiert, und alles andere läuft unverändert:
# eine Überweisung ist kein Fallback, sondern der B2B-Normalfall.
stripe==11.2.0
pytest==8.3.4
pytest-asyncio==0.24.0
